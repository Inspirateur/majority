use std::collections::HashMap;

pub fn compute_ranking(votes: &Vec<HashMap<usize, usize>>) -> Vec<usize> {
    // Given a list of votes (for each option), compute the ranking of each option
    todo!()
}
